import { connect } from 'react-redux';

const connectRedux = (mapStateToProps) => (WrappedComponent) => {

  const mapDispatchToProps = (dispatch) => {

  };

  return <WrappedComponent  />
};

export default connectRedux;