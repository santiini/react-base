import React, { Component } from 'react';
import { Button } from 'antd';

export default class Detail extends Component {
  render() {
    return (
      <div>
        <h4>Deatil</h4>
        <Button>/Containers/Detail</Button>
      </div>
    )
  }
}